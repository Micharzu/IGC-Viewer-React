import React, { useState } from "react";
import { Bar, Line } from "react-chartjs-2";

const Graph = () => {
  return (
    <div className="graph">
      <h1>GRAPH</h1>
    </div>
  );
};

export default Graph;
